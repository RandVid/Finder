/**
 * Placeholder for teammate's user stats feature.
 * Replace the body with real stats when GET /stats/users/:id (or similar) lands.
 */
export default function UserStatsPlaceholder() {
  return (
    <section className="rounded-xl border border-dashed border-gray-300 bg-gray-50 p-4">
      <h3 className="text-sm font-semibold text-gray-700 mb-1">Stats</h3>
      <p className="text-sm text-gray-500">
        Activity stats for this user will appear here once the stats feature is merged.
      </p>
    </section>
  )
}
